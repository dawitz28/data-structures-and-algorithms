'use strict';

const kthFromEnd = require('../index.js');

describe('Linked list kth from the end', () => {
  it('should determined if k is greater than the linkedList.');
  ('should throw an error when k and the length of the list are the same.');
  ('should return when k is not a positibe integer');
  ('should return when the linked list is of a size 1.');
  ('should return when k is in the middle of the linked list.');


});